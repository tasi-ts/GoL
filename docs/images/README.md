# Screenshot captures

Drop real pygame window captures here (PNG or GIF). The root README links to:

- `grid.png` — bounded or toroidal 2D grid
- `sphere.png` — geodesic sphere view

Do not commit generated placeholders; capture the running `gol` window locally.
